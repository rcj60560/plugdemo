ext {
    username = "luochenjian"
    // 是否是发布模式 所有模块都是lib/apk
    isRelease = true

    app_android = [
            compileSdkVersion: 30,
            buildToolsVersion: "30.0.3",
            minSdkVersion    : 21
    ]

}